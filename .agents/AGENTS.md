# Workspace Rules - Android Emulator Project

- Always run commands within the `.venv` Python virtual environment (`source .venv/bin/activate`).
- Store secret keys in `.env` (never commit `.env` to git).
- Default to `gemini-flash-latest` model for multimodal device control.
